import { makeStyles, Theme } from "@material-ui/core";

export const useBracketOverviewStyles = makeStyles((theme: Theme) => ({
  root: {
    display: "flex",
    alignItems: "stretch",
    height: "100%",
  },
}));
